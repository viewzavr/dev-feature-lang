попробовать сделать так чтобы параметры были прямо в объекте т.е. 
не obj.params.alfa а obj.alfa и obj.alfa=

pro: удобнее писать js-коды..

update: также совместить в общем пр-ве имен с событиями и методами (кои можно вызывать как методы obj.compute(), как кнопки при желании, и соединять с событиями через тпу)